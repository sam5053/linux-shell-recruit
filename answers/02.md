1. `./tools/recruit-info` 可以运行，是因为 `./` 给了 Shell 一个明确的相对路径，Shell 直接按这个路径找到文件并执行。
2. 直接输入 `recruit-info` 时，Shell 只会去 `PATH` 里列出的目录里搜索；如果 `tools/` 不在 `PATH` 中，就会提示 `command not found`。
3. `ls` 能直接运行，是因为它的可执行文件在 `PATH` 里的某个目录中，比如 `/usr/bin/ls`，Shell 会按 `PATH` 顺序找到并运行它。
